# 줄바꿈을 플랫폼에 따라 흔들리지 않게 고정한다.
* text=auto

# PowerShell 스크립트는 CRLF 로 유지한다.
# (UTF-8 BOM 과 함께여야 Windows PowerShell 5.1 이 한글을 제대로 읽는다)
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 셸 스크립트는 LF 여야 실행된다.
*.sh  text eol=lf
gradlew text eol=lf
